A React weather dashboard that retrieves live weather data from the OpenWeather API and enriches the experience with dynamic backgrounds and location-based visualization. The application uses custom hooks, reusable components, and separated API logic to maintain a clean structure between data handling and presentation.

Fetches and displays live weather information
Uses custom React hooks for API calls, state management, and async flows
Separates API communication and weather data mapping
